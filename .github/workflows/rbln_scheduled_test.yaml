name: Optimum-rbln / Scheduled Test

on:
  schedule:
    # Run every day at 1 (16:00 UTC, 1:00am KST)
    - cron: '0 16 * * *'
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - dev

jobs:
  dispatch:
    uses: rbln-sw/optimum-rbln/.github/workflows/rbln_scheduled_test_impl.yaml@ci/schedule_dispatch
    secrets: inherit
